import { Testq } from "../getCurrency";

test("expect", () => {
  expect(Testq()).toBe(false);
});

//МОЖЕТ импорты не работают 
// https://dev.to/hannahadora/jest-testing-with-vite-and-react-typescript-4bap
