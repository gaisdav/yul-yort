import { makeStyles } from "@material-ui/core/styles";

const useStyles = makeStyles({
  resultCard: {
    margin: 24,
  },
  resultCardContent: {
    paddingTop: 0,
  },
});

export default useStyles;
